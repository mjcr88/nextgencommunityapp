{
  "name": "ecovilla-platform",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "clean": "turbo run clean",
    "type-check": "turbo run type-check",
    "validate-setup": "turbo run lint build type-check"
  },
  "devDependencies": {
    "@vitest/expect": "^3.2.4",
    "@vitest/ui": "^3.2.4",
    "turbo": "^1.12.0",
    "vitest": "^3.2.4"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "dependencies": {
    "@supabase/ssr": "^0.7.0",
    "@supabase/supabase-js": "^2.74.0",
    "clsx": "^2.1.1",
    "tailwind-merge": "^2.5.2"
  }
}
